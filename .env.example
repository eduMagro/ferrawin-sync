# =====================================================================
# FERRAWIN SYNC - Configuración
# =====================================================================
# Copiar a .env y configurar los valores

# Base de datos FerraWin (SQL Server)
FERRAWIN_HOST=192.168.0.7
FERRAWIN_PORT=1433
FERRAWIN_DATABASE=FERRAWIN
FERRAWIN_USERNAME=sa
FERRAWIN_PASSWORD=Ferrawin73

# Servidor LOCAL (desarrollo)
LOCAL_URL=http://127.0.0.1/manager/public/
LOCAL_TOKEN=tu-token-local-aqui

# Servidor de PRODUCCIÓN
PRODUCTION_URL=https://tu-dominio-produccion.com/
PRODUCTION_TOKEN=tu-token-produccion-aqui

# Configuración de sincronización
# Días atrás por defecto (se puede sobrescribir con CLI)
SYNC_DIAS_ATRAS=7
# Comprimir datos con gzip antes de enviar
SYNC_COMPRESS=true

# Logging (debug, info, warning, error)
LOG_LEVEL=info

# =====================================================================
# USO DEL SCRIPT
# =====================================================================
# php sync-optimizado.php --año 2024 --target local        # Sync a local
# php sync-optimizado.php --año 2024 --target production   # Sync a producción
# php sync-optimizado.php --test 5 --target local          # Probar con 5 planillas
# php sync-optimizado.php --todos --target production      # Migrar todo a prod
# php sync-optimizado.php --año 2025 --desde-codigo 2025-007816 --target local
